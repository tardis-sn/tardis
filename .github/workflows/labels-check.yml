name: Label Check

on:
  pull_request_target:
    types: [labeled]

jobs:
  check-and-manage-labels:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      issues: write
      contents: read
    
    steps:
      - name: Check for sensitive label
        if: github.event.label.name == 'CI/CD'
        run: echo "Sensitive label CI/CD was added, will add warning and remove it"

      - name: Find existing comment
        uses: peter-evans/find-comment@v1
        id: fc
        if: github.event.label.name == 'CI/CD'
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body-includes: Warning - LFS bandwidth usage

      - name: Create or update comment
        uses: peter-evans/create-or-update-comment@v1
        if: github.event.label.name == 'CI/CD'
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          comment-id: ${{ steps.fc.outputs.comment-id }}
          edit-mode: replace
          body: |
            Warning - LFS bandwidth usage
            ⚠️ **Warning**: The label you applied triggers workflows that consume LFS bandwidth and should be used carefully. For safety, this label will be automatically removed once the checks have run.

      - name: Remove label
        if: github.event.label.name == 'CI/CD'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            await github.rest.issues.removeLabel({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
              name: 'CI/CD'
            });
