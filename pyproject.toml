[build-system]
requires = ["setuptools",
            "setuptools_scm",
            "wheel"]
build-backend = 'setuptools.build_meta'

[tool.black]
line-length = 80
target-version = ['py36']
extend-exclude = '''
(
  /(
    model           # Temporary - to remove later
    | montecarlo    # Temporary
  )/
  | setup.py
  | docs/conf.py
  | _astropy_init.py
)
'''
