# TARDIS Version Mark Admonition Text
# Edit these texts to customize the version-specific admonition messages

[rtx]
title = "TARDIS-RTX Architecture"
text = """TARDIS uses a modular architecture that separates topology, physics, and simulation 
into distinct, composable layers. This architecture enables flexible model construction, 
independent testing of components, and clear separation of concerns."""

[classic]
title = "TARDIS Classic Architecture"
text = """This guide uses the classic TARDIS configuration-based approach where model 
parameters are specified in YAML configuration files. For the new modular architecture, 
see the RTX documentation."""
